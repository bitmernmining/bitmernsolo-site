# Public site env (no secrets)

# Plausible Analytics - set to the domain registered in Plausible
# (typically www.bitmernsolo.com). When unset, the tracking script is not loaded.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=www.bitmernsolo.com
