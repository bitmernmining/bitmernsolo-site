# Public site env (no secrets)

# Plausible Analytics - must match the domain registered in Plausible.
# Plausible Cloud normalizes www away — use apex. Layout also strips a
# leading www. When unset, the tracking script is not loaded.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=bitmernsolo.com
