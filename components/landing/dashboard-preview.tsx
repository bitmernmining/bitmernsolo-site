"use client";

/**
 * Temporary stub — the full interactive mockup was corrupted on main
 * (path-string placeholder). Restoring the 10-coin version next.
 * Keeping a valid export so production builds stay green.
 */
export function DashboardPreview() {
  return null;
}
